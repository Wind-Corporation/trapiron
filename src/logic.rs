//! Description of hardcoded game logic.
//!
//! This container is modified at startup only. Game state resides in
//! [`World`](crate::world::World).

pub struct Logic {
    // TODO
}

impl Logic {
    /// Create a new instance of `Logic`.
    pub fn new() -> Self {
        Self {}
    }
}
